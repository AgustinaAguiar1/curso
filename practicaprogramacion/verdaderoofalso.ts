import * as ReadlyneSync from 'readline-sync';

let haceFrio : Boolean=true;
let haceCalor : Boolean= false;

if (haceFrio) {
    if (haceCalor) {
        console.log ('Me quedo en casa');
    } else {
        console.log ('Vengo al curso del CFL');
}
}